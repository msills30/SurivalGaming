extends Resource
class_name SettingsReource

#For this to save properly we must use the export variable 
@export var music_volume := 1
@export var sfx_volume := 1
@export var res_scale := 1
@export var ssaa_enabled := false
@export var full_screen_enabled := true

